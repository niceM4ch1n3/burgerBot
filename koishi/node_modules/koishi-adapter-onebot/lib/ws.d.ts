import { App, Adapter } from 'koishi-core';
import { connect } from './utils';
import { CQBot } from './bot';
import WebSocket from 'ws';
export declare class WsClient extends Adapter.WsClient<'onebot'> {
    connect: typeof connect;
    constructor(app: App);
    prepare(bot: CQBot): WebSocket;
}
export declare class WsServer extends Adapter<'onebot'> {
    wsServer?: WebSocket.Server;
    constructor(app: App);
    start(): Promise<void>;
    stop(): void;
}
